# ── Server ────────────────────────────────────────────────────────────────────
BACKEND_PORT=4000

# ── MongoDB ───────────────────────────────────────────────────────────────────
# Local:  mongodb://localhost:27017
# Docker: mongodb://mongo:27017
MONGODB_URI=mongodb+srv://edwardsyambasu_db_user:bxhuqJ83mhFQG78K@cluster0.nwnbuqt.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=shop_keeper_main

# ── Security ──────────────────────────────────────────────────────────────────
# Strong random secret for JWT signing — CHANGE THIS in production!
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=helping-Africa-to-become-self-sufficient-in-food-production
JWT_EXPIRES_IN=30d

# Optional API key required on all /api/sync/* requests (leave empty to disable)
SYNC_API_KEY=d8796b54556cdd5c9190d7cd27797fe86cea6e304eb76c0e5176a49a1571c07a

# Set false only if you want to block remote delete sync operations
ENABLE_REMOTE_SYNC_DELETE=true

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
FRONTEND_ORIGIN=https://swiftpos-iota.vercel.app
